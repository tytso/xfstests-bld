
# TODO: can this live somewhere else?

function get_arch() {
    case "$1" in
	x86-64|x86_64|X86_64|amd64)
	    ARCH="amd64"
	    GCE_ARCH="X86_64"
	    ;;
	arm64)
	    ARCH="arm64"
	    GCE_ARCH="ARM64"
	    ;;
	*)
	    echo "Architecture $ARCH not supported"
	    exit 1
	    ;;
    esac
}
